{{ form_start(form) }}
    {{ form_row(form.email) }}
    {{ form_row(form.firstname) }}
    {{ form_row(form.lastname) }}
    {{ form_row(form.password) }}
    {{ form_row(form.audit) }}
    {{ form_row(form.isActive) }}
    {{ form_row(form.role) }}
    

    <a class="btn btn-secondary" href="{{ path('user_index') }}"><i class="fa fa-arrow-circle-left"></i> {{'Back to list'|trans}}</a>
    {% if is_granted('ROLE_ADMIN') %}
        <button class="btn btn-primary"><i class="fa fa-check"></i> {{ button_label|default('Save'|trans) }}</button>
    {% endif %}

{{ form_end(form) }}
